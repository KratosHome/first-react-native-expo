import {StyleSheet} from "react-native";

export const globalStyle = StyleSheet.create({
    main: {
        flex: 1,
    },
    title: {
        fontSize: 20,
        color: "#333",
        fontFamily: "mt-bold",
        textAlign: "center"
    }
});
